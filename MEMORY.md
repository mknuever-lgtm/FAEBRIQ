# FÆBRIQ Working Memory — append-only log

## 2026-08-17
- Created CLAUDE.md and MEMORY.md for session persistence. (Note: this entry was originally misdated 2026-08-13; corrected.)
- Repo's default branch is named `mknuever-lgtm-patch-1`, not `main` — an old auto-generated name from a one-off GitHub web edit that got left as default. Functions fine, just an unusual name. Renaming the actual GitHub "default branch" setting isn't reachable from this session's GitHub tools (no repo-admin/settings endpoint); would need the GitHub web UI (Settings → Branches) or a session with broader GitHub scope.
- Every Claude Code web session works on its own auto-named branch (e.g. `claude/great-wozniak-Ke10Y`); it only lands in the real project once merged via PR. Found 6 old leftover session branches never cleaned up — 5 were stale/fully superseded by later work already on the default branch. One (`claude/shopify-product-progress-o8wxkv`) contained a real, unmerged FÆBRIQ Brand Kit (`faebriq-brand-kit/SKILL.md` + `references/`) — voice, visual identity, color/type tokens, sample copy — recovered and merged into the default branch. CLAUDE.md's brand-voice section now points to it directly.
- **Brand system conflict, unresolved:** `faebriq-brand-kit/SKILL.md` (merged today) specifies Playfair Display + pride-flag hex, and explicitly calls Instrument Serif "retired/wrong." But the live, implemented tokens (`tokens/fonts.css`, `tokens/typography.css`, `tokens/colors.css` — untouched since the original design-system commits) still run Instrument Serif + Archivo, a blue `#4a9eff` interactive accent, and a different ROYGBIV hex set than the brand-kit's. Nothing in `components/`, `ui_kits/`, or the guideline cards was ever migrated to Playfair. Maurice hasn't picked a side yet — don't generate new brand assets against either system until he does, or it becomes a third/fourth variant.
- **This session's network policy blocks `cdn.shopify.com`** (both raw `curl` and `WebFetch` get `EGRESS_BLOCKED`/403 from the proxy) — can pull product/image *metadata* live via the Shopify MCP tools, but cannot view image pixels directly. Any visual claim about a specific image needs either the user's own description or a prior session's confirmed finding; don't assert pixel-level content from filenames/alt-text alone.
- **Product image audit (Aug 19):** all 6 active apparel products (both tees, crewneck, hoodie, tote, cap) carry the identical structure — 8 images each: a generic/untitled "legacy" batch (uploaded Jul 29, positions 1–4, likely flat/design-file renders — Printify-pattern filenames) followed by a properly-labelled Manus-generated batch (uploaded Aug 14, positions 5–8: clean product → ¾ on-model → lifestyle → macro). The good batch is stranded behind the legacy one and behind the still-unpublished theme's "only image #1 renders" bug. Recommended fix (not yet executed, needs Maurice's go-ahead): reorder each gallery so the Aug 14 batch leads. Stickers (6 listings) are a separate, lower-priority pattern: 2–3 images each from a single Aug 5 batch, empty alt text, no scale/context shots; "Deploying Identity v2.0" sticker has only 2 images vs. 3 on every sibling. Full findings published as an artifact: https://claude.ai/code/artifact/3aed4d9a-2205-4682-b3ec-01627bbbc03f
- Known-bug carry-forward, observed in passing while pulling live product data: the orphan White/L tee variant from CLAUDE.md's bug list is no longer on either *active* tee product (only Black S–5XL on both) — it only still exists on an archived/zero-inventory duplicate product, so it's likely already resolved on the live catalog but wasn't independently re-verified end-to-end. The mug is confirmed fully gone (not in active or archived listings). Did not check the cross-copied-SEO-titles bug this pass — that's the page/meta `seo.title` field specifically, not the product title or description, and wasn't part of this pull.
